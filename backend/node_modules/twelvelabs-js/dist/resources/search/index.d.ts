import { RequestOptions } from '../../core';
import * as Models from '../../models';
import { APIResource } from '../../resource';
import { SearchOptions } from './interfaces';
export declare class Search extends APIResource {
    query({ indexId, query, queryText, queryMediaType, queryMediaFile, queryMediaUrl, options: searchOptions, groupBy, threshold, operator, conversationOption, filter, pageLimit, sortOption, adjustConfidenceLevel, }: SearchOptions, options?: RequestOptions): Promise<Models.SearchResult>;
    byPageToken(pageToken: string, options?: RequestOptions): Promise<Models.SearchResult>;
}
//# sourceMappingURL=index.d.ts.map